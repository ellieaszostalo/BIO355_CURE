vcftools --vcf variants_4817snps_123inds.vcf \
 --keep ../data_processed/popORS.txt \
 --freq2 \
 --out popORS_maf

#had to change the code as the format of the data was not the standard 
#The last line of code would need to be changed depending on the population of intrest
awk 'NR > 1 {
    # ID MAF by smaller of 2 columns
    if ($5 < $6) 
        maf = $5; 
    else 
        maf = $6;

    sum += maf; 
    n++
} 
END {
    # Output only the final calculation
    print "Mean MAF: " sum/n;
}' popCA_maf.frq
